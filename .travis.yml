language: cpp
dist: xenial
sudo: false

matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
      script:
        - CXX=g++-8 cmake .
        - cmake --build .

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - clang-8
      script:
        - CXX=clang++-8 cmake .
        - cmake --build .
